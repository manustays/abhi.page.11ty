---
permalink: /quay/
title: Quay — Your local dev stack in one menu bar
description: Start, stop and monitor local dev servers, Homebrew services, Docker containers and AI coding agents from one macOS menu bar app — with live health, CPU and memory, port-conflict detection, and an amber glyph when an agent is waiting on you. Free and open source.
keywords: quay, macos menu bar app, dev services manager, localhost ports, docker, homebrew, claude code, codex cli, ai coding agent monitor, agent session status
generate_social_image: false
image: /assets/img/quay/social-preview.png
---

{% extends "./_layouts/base.njk" %}
{% from "components/desktop-frame.njk" import desktopFrame %}

{% block custom_style %}
	{% set css_product %}{% include "css/product.css" %}{% endset %}
	{{ css_product | cssmin | safe }}
{% endblock %}

{% set repo = "https://github.com/manustays/quay" %}
{% set download = "https://github.com/manustays/quay/releases/latest" %}
{% set brew = "brew install --cask manustays/tools/quay" %}

{% block main %}
<div class="product">

	{# ---------- Hero ---------- #}
	<section class="block product-hero">
		<div class="product-hero-grid">
			<div class="product-hero-copy">
				<div class="product-hero-head">
					<img class="product-logo" src="/assets/img/quay/logo.svg" alt="" width="88" height="88" />
					<div class="product-hero-titles">
						<div class="product-wordmark">Quay</div>
						<div class="product-pronounce mono">/kiː/ · where your ports come in</div>
					</div>
				</div>

				<h1 class="product-headline">Your entire local dev stack.<br><span class="hl">One menu bar.</span></h1>

				<p class="product-lede">Start, stop and monitor dev servers, Homebrew services, Docker containers and AI coding agents from one lightweight macOS app — with live health, CPU and memory, port-conflict detection, and an amber glyph the moment an agent is waiting on you.</p>

				<div class="product-cta">
					<a class="button" href="{{ download }}" target="_blank" rel="noopener">
						{% svgico 'arrow-circle-down' %}Download for macOS
					</a>
					<a class="button secondary" href="{{ repo }}" target="_blank" rel="noopener">
						{% svgico 'github' %}View on GitHub
					</a>
				</div>

				<p class="product-install">
					<span>or with Homebrew</span>
					<code>{{ brew }}</code>
				</p>

				<p class="product-note">
					<svg class="product-note-ico" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><circle cx="12" cy="12" r="9"/><line x1="12" y1="11" x2="12" y2="16"/><circle cx="12" cy="8" r="0.6" fill="currentColor" stroke="none"/></svg>
					One universal build for Apple&nbsp;Silicon and Intel. Releases aren't notarized yet, so the first launch needs System Settings → Privacy &amp; Security → <em>Open Anyway</em> — right-click → Open no longer works on recent macOS. Fully open source under MIT.
				</p>

				<ul class="product-badges">
					<li>macOS</li>
					<li>Universal</li>
					<li>Rust + TypeScript</li>
					<li>Tauri v2</li>
					<li>MIT</li>
				</ul>
			</div>

			<div class="product-hero-visual">
				{{ desktopFrame("/assets/img/quay/popup.png", "Quay menu bar popup listing running dev services with live status, CPU and memory metrics.", transparent=true) }}
			</div>
		</div>
	</section>

	{# ---------- Positioning callout ---------- #}
	<section class="block product-callout product-highlight-bg product-callout-text">
			<span class="product-callout-dot" aria-hidden="true"></span>
			Not just a port monitor — <span class="hl">a control center for everything running locally.</span>
	</section>

	<div class="product-highlight-bg">

		{# ---------- High-value workflows ---------- #}
		<section class="block">
			<h2 class="product-section-title">What a good local day looks like</h2>
			<ul class="product-workflows">
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'flow-bolt' %}</span>
					<h3>Start your stack in seconds</h3>
					<p>Your frontend, API and Redis sit together as one group — start them from the menu bar in seconds, no terminal tabs to hunt down.</p>
					<ul class="product-workflow-tags">
						<li>Project servers</li>
						<li>Homebrew services</li>
						<li>Auto-detection</li>
					</ul>
				</li>
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'feature-radar' %}</span>
					<h3>Find what stole your port</h3>
					<p>Port radar spots the unmanaged process on a port. Open it in the browser to identify it, then adopt it into Quay or terminate it.</p>
					<ul class="product-workflow-tags">
						<li>Port radar</li>
						<li>One-click browser</li>
						<li>Live status &amp; health</li>
					</ul>
				</li>
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'feature-metrics' %}</span>
					<h3>Keep heavy services visible</h3>
					<p>Watch CPU, memory, uptime and health right from the menu bar — no need to open Activity Monitor.</p>
					<ul class="product-workflow-tags">
						<li>Resource metrics</li>
						<li>Live status &amp; health</li>
						<li>Unified management</li>
					</ul>
				</li>
			</ul>
		</section>

		{# ---------- Agent radar ---------- #}
		<section class="block">
			<h2 class="product-section-title">Your coding agents, on the same shelf</h2>
			<p class="product-compare-lede">Claude Code, Codex CLI, OpenCode and Pi sessions appear beside your services — so an agent that finished twenty minutes ago, or one stuck at a permission prompt, isn't a terminal window you forgot to check.</p>
			<ul class="product-workflows">
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'trust-local' %}</span>
					<h3>It never reads your transcripts</h3>
					<p>Each agent reports its own lifecycle through a tiny hook, so Quay never opens the session files under <code>~/.claude</code>, <code>~/.codex</code> or <code>~/.pi</code>. Nothing about your prompts or projects is read, stored or sent.</p>
					<ul class="product-workflow-tags">
						<li>Claude Code</li>
						<li>Codex CLI</li>
						<li>OpenCode</li>
						<li>Pi</li>
					</ul>
				</li>
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'feature-bell' %}</span>
					<h3>Know when one needs you</h3>
					<p>Working, idle, or waiting on you at a permission prompt. The menu bar glyph turns amber while the popover is shut, so a blocked agent doesn't sit there unanswered while you're in another window.</p>
					<ul class="product-workflow-tags">
						<li>Working / idle / waiting</li>
						<li>Works popover-closed</li>
						<li>Optional count</li>
					</ul>
				</li>
				<li class="product-workflow">
					<span class="ico" aria-hidden="true">{% svgico 'feature-terminal' %}</span>
					<h3>Jump straight back in</h3>
					<p>One click focuses that session's own terminal. Sessions in the same folder club into a single project row, with the session name on hover and live CPU, memory and uptime per agent.</p>
					<ul class="product-workflow-tags">
						<li>Ghostty, Kitty, WezTerm</li>
						<li>iTerm2, Terminal.app</li>
						<li>Reveal, kill, ignore</li>
					</ul>
				</li>
			</ul>
			<p class="product-note">
				<svg class="product-note-ico" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><circle cx="12" cy="12" r="9"/><line x1="12" y1="11" x2="12" y2="16"/><circle cx="12" cy="8" r="0.6" fill="currentColor" stroke="none"/></svg>
				Hooks install in one click from Settings — no terminal, and no restarting the sessions you already have open; a running session appears as soon as it next reports. Until an agent's hooks are installed its sessions stay invisible, and the Agents list tells you so. Jump-to-session also covers Cmux, Herdr and Supacode; it needs remote control switched on for Kitty, and doesn't reach bare tmux, screen or zellij panes.
			</p>
		</section>

		{# ---------- Five kinds of service ---------- #}
		<section class="block">
			<h2 class="product-kinds-title">Five kinds of service. One place.</h2>
			<ul class="product-kinds is-five">
				<li class="product-kind">
					<span class="ico" aria-hidden="true">{% svgico 'feature-services' %}</span>
					<h3>Project servers</h3>
					<p>Next.js, Vite, Django, Rails — auto-detected from your project config and grouped into one stack.</p>
				</li>
				<li class="product-kind">
					<span class="ico" aria-hidden="true">{% svgico 'feature-brew' %}</span>
					<h3>Homebrew services</h3>
					<p>Redis, Postgres, MySQL, MongoDB. Start and stop brew services without touching the terminal.</p>
				</li>
				<li class="product-kind">
					<span class="ico" aria-hidden="true">{% svgico 'feature-docker' %}</span>
					<h3>Docker containers</h3>
					<p>Pick an image from the ones you've already pulled. Quay starts the daemon if it needs to, and reuses an existing container rather than discarding its data.</p>
				</li>
				<li class="product-kind">
					<span class="ico" aria-hidden="true">{% svgico 'feature-terminal' %}</span>
					<h3>Terminal &amp; CLI tools</h3>
					<p>Long-running interactive tools you register yourself, started and stopped like any other service. Agent sessions you launched in your own terminal show up in the agent radar instead.</p>
				</li>
				<li class="product-kind">
					<span class="ico" aria-hidden="true">{% svgico 'feature-stack' %}</span>
					<h3>Daemons with their own CLI</h3>
					<p>Anything driven by a start and stop command pair — a launchd service, a local model server. Status is polled even while it's stopped, so a change you made outside Quay still shows up.</p>
				</li>
			</ul>
		</section>

		{# ---------- Comparison ---------- #}
		<section class="block">
			<h2 class="product-section-title">More than a port monitor</h2>
			<p class="product-compare-lede">Most tools stop at listening ports. Quay manages the whole stack — the rows below are where it pulls ahead.</p>

			<div class="product-compare-card">
				<div class="product-compare-scroll">
					<table class="product-compare">
						<thead>
							<tr>
								<th scope="col">Capability</th>
								<th scope="col">Typical port monitor</th>
								<th scope="col" class="product-compare-us">
									<img class="product-compare-logo" src="/assets/img/quay/logo.svg" alt="" width="20" height="20" />Quay
								</th>
							</tr>
						</thead>
						<tbody>
							{% set compareRows = [
								{ cap: "See listening ports",             typical: "Yes",  edge: false },
								{ cap: "Kill port / process",             typical: "Yes",  edge: false },
								{ cap: "Manage project servers",          typical: "Some", edge: false },
								{ cap: "Homebrew services",               typical: "—",    edge: true  },
								{ cap: "Docker containers",               typical: "Some", edge: true  },
								{ cap: "Terminal &amp; CLI services",     typical: "—",    edge: true  },
								{ cap: "CPU &amp; memory per service",    typical: "—",    edge: true  },
								{ cap: "HTTP health checks",              typical: "Some", edge: false },
								{ cap: "Auto-detect project config",      typical: "Some", edge: false },
								{ cap: "Adopt unmanaged processes",       typical: "Some", edge: false },
								{ cap: "Group full stacks",               typical: "—",    edge: true  },
								{ cap: "AI agent session states",         typical: "—",    edge: true  },
								{ cap: "Waiting-for-you menu bar signal", typical: "—",    edge: true  },
								{ cap: "Jump to the agent's terminal",    typical: "—",    edge: true  }
							] %}
							{% for row in compareRows %}
								<tr{% if row.edge %} class="is-edge"{% endif %}>
									<th scope="row">{{ row.cap | safe }}</th>
									<td class="product-compare-typical">{{ row.typical | safe }}</td>
									<td class="product-compare-yes">
										<span class="ico" aria-hidden="true">{% svgico 'check' %}</span>
										<span class="visually-hidden">Yes</span>
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			</div>
		</section>

		{# ---------- Measured efficiency ---------- #}
		<section class="block">
			<h2 class="product-section-title">Measured, not claimed</h2>
			<p class="product-compare-lede">An app you never quit should cost nothing while you aren't looking at it. Numbers from a debug build with seven services registered, sampled by a script that ships in the repo.</p>

			<div class="product-compare-card">
				<div class="product-compare-scroll">
					<table class="product-compare product-efficiency">
						<thead>
							<tr>
								<th scope="col">What you're doing</th>
								<th scope="col">CPU</th>
								<th scope="col">Interface</th>
								<th scope="col">Idle wakeups</th>
							</tr>
						</thead>
						<tbody>
							{% set efficiencyRows = [
								{ state: "Sitting in the menu bar — the everyday case", cpu: "0.10%", renderer: "0.00%", wakeups: "0.0 / min", edge: true  },
								{ state: "Popover open, watching your services",        cpu: "0.45%", renderer: "0.51%", wakeups: "4.0 / min", edge: false }
							] %}
							{% for row in efficiencyRows %}
								<tr{% if row.edge %} class="is-edge"{% endif %}>
									<th scope="row">{{ row.state | safe }}</th>
									<td>{{ row.cpu | safe }}</td>
									<td>{{ row.renderer | safe }}</td>
									<td>{{ row.wakeups | safe }}</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			</div>

			<p class="product-footnote">
				These come from a debug build; a release bundle reads several times lower, so treat them as the pessimistic figure. Absolutes depend on how many services you've registered — the comparable number is the difference between the two rows, and opening the popover costs about 0.35 percentage points of CPU in this configuration. Reproduce it with <code>scripts/cpu-check.sh</code>.
			</p>

			<p class="product-footnote">
				<strong>And it stops when you do.</strong> Polling parks entirely while every display is asleep or the Mac is locked — overnight with the lid shut, Quay runs no status passes rather than roughly 1,200 an hour. There's no setting for it; it's the default.
			</p>
		</section>

	</div>

	{# ---------- How it works ---------- #}
	<section class="block bg-shaded">
		<h2 class="product-section-title">Up and running in a minute</h2>
		<ol class="product-steps">
			<li class="product-step">
				<h3>Install</h3>
				<p>Run <code>{{ brew }}</code>, or download the <code>.dmg</code> from Releases and drag Quay to <code>/Applications</code>.</p>
			</li>
			<li class="product-step">
				<h3>Add a service</h3>
				<p>Hit <code>+ Add</code>, pick a project folder — Quay auto-fills the start command and port.</p>
			</li>
			<li class="product-step">
				<h3>Control it</h3>
				<p>Start, stop, open in the browser or jump to a terminal — all from the menu bar.</p>
			</li>
			<li class="product-step">
				<h3>Let it keep itself current</h3>
				<p>Updates are signed with their own key, independent of Apple notarization — so in-app updating works even on today's unsigned builds.</p>
			</li>
		</ol>
	</section>

	{# ---------- Open-source trust ---------- #}
	<section class="block product-trust">
		<h2 class="product-section-title">Open source you can trust</h2>
		<ul class="product-trust-grid">
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-license' %}</span>
				<h3>MIT licensed</h3>
				<p>A permissive license — use, fork and ship it however you like.</p>
			</li>
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-local' %}</span>
				<h3>Fully local</h3>
				<p>Your services and settings never leave your Mac, and Quay never reads your agents' session files — their states come from the agents themselves.</p>
			</li>
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-no-account' %}</span>
				<h3>No account</h3>
				<p>No sign-up, no login. Download it and go.</p>
			</li>
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-no-telemetry' %}</span>
				<h3>No telemetry</h3>
				<p>No tracking, no analytics, nothing phoned home about what you run. The only outbound traffic is GitHub Releases, to check for and download updates.</p>
			</li>
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-code' %}</span>
				<h3>Rust + TypeScript</h3>
				<p>A Tauri app: a fast Rust core with a TypeScript interface.</p>
			</li>
			<li class="product-kind">
				<span class="ico" aria-hidden="true">{% svgico 'trust-lightweight' %}</span>
				<h3>Lightweight</h3>
				<p>Native and lean — no Electron, no bundled browser.</p>
			</li>
		</ul>
		<div class="product-trust-links">
			<a href="{{ repo }}" target="_blank" rel="noopener">View source</a>
			<a href="{{ download }}" target="_blank" rel="noopener">Current release</a>
			<a href="{{ repo }}/issues" target="_blank" rel="noopener">Contributions welcome</a>
		</div>
	</section>

	{# ---------- Support / sponsor ---------- #}
	<section class="block product-support bg-shaded">
			<span class="product-support-ico" aria-hidden="true">{% svgico 'heart' %}</span>
			<h2 class="product-section-title product-support-title">Enjoying Quay?</h2>
			<p class="product-support-text">Quay is free and open source, built in my spare time. If it saves you time, a one-time sponsorship — or even a quick star — genuinely helps and keeps me motivated.</p>
			<div class="product-cta product-support-cta">
				<a class="button sponsor" href="https://github.com/sponsors/manustays?frequency=one-time" target="_blank" rel="noopener">
					{% svgico 'heart' %}Sponsor on GitHub
				</a>
				<a class="button secondary" href="{{ repo }}" target="_blank" rel="noopener">
					{% svgico 'github' %}Star on GitHub
				</a>
			</div>
			<p class="product-support-note">No pressure — Quay stays free either way.</p>
	</section>

	{# ---------- Closing CTA ---------- #}
	<section class="block product-closing">
		<div class="product-cta">
			<a class="button" href="{{ download }}" target="_blank" rel="noopener">
				{% svgico 'arrow-circle-down' %}Download for macOS
			</a>
		</div>
		<p class="product-license">MIT © 2026 Kumar Abhishek</p>
	</section>

</div>
{% endblock %}
